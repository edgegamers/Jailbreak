## When updating Gangs do the following:

- Build Gangs repo
- Copy GangsAPI.dll into this folder
- Copy GangsAPI.dll from Gangs build folder into the servers game\csgo\addons\counterstrikesharp\shared\GangsAPI\GangsAPI.dll folder
  If GangsAPI does not exist in the servers shared folder, create it.
